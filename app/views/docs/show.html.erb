<h1> <%= @doc.title %></h1>
<p> <%= @doc.content %></p>

 <%= link_to 'All docs', docs_path %>
 <%= link_to 'Edit', edit_doc_path(@doc) %>
 <%= link_to 'Remove', doc_path(@doc), method: :delete, data: { confirm: 'Are you sure?' } %>
